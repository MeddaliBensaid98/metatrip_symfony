 {% extends "base.html.twig" %} 
 


 {% block stylesheet %}

#container {
  height: 400px;
}

.highcharts-figure,
.highcharts-data-table table {
  min-width: 310px;
  max-width: 800px;
  margin: 1em auto;
}

#datatable {
  font-family: Verdana, sans-serif;
  border-collapse: collapse;
  border: 1px solid #ebebeb;
  margin: 10px auto;
  text-align: center;
  width: 100%;
  max-width: 500px;
}

#datatable caption {
  padding: 1em 0;
  font-size: 1.2em;
  color: #555;
}

#datatable th {
  font-weight: 600;
  padding: 0.5em;
}

#datatable td,
#datatable th,
#datatable caption {
  padding: 0.5em;
}

#datatable thead tr,
#datatable tr:nth-child(even) {
  background: #f8f8f8;
}

#datatable tr:hover {
  background: #f1f7ff;
}
   {% endblock %}


   {% block body %}



<figure class="highcharts-figure">
  <div id="container"></div>
<br>
    <div id="cc"></div>
  <p class="highcharts-description"   style="visibility:hidden;">
    Chart showing how an HTML table can be used as the data source for the chart using the Highcharts data module. The chart is built by referencing the existing HTML data table in the page. Several common data source types are available, including CSV and Google Spreadsheet.
  </p>

   <table id="datatable3" style="visibility:hidden;">
  
        <thead>
            <tr>
                <th></th>
                <th>Nombre de Voyage organise</th>
            
            </tr>
        </thead>
        <tbody>
              {% for y in lista3 %}
            <tr>
                <th>'{{y.pays}}'</th>
                <td>{{y.nb}}</td>
      
            </tr>
         
                      {% endfor %}

        </tbody>
    </table>


  <table id="datatable2" style="visibility:hidden;">
  
        <thead>
            <tr>
                <th></th>
                <th>Nombre de reservations</th>
            
            </tr>
        </thead>
        <tbody>
              {% for l in lista %}
            <tr>
                <th>'{{l.pays}}'</th>
                <td>{{l.nb}}</td>
      
            </tr>
         
                      {% endfor %}

        </tbody>
    </table>
</figure>


<br>x
   {% block javascripts %}

<script src="https://code.highcharts.com/highcharts.js"></script>
<script src="https://code.highcharts.com/modules/data.js"></script>
<script src="https://code.highcharts.com/modules/exporting.js"></script>
<script src="https://code.highcharts.com/modules/accessibility.js"></script>
  <script src="https://code.jquery.com/jquery-1.12.4.js"></script> 
  <script>
  Highcharts.chart('container', {
  data: {
    table: 'datatable2'
  },
  chart: {
    type: 'area'
  },
  title: {
    text: 'Voyage le plus/moins réservé'
  },
  yAxis: {
    allowDecimals: false,
    title: {
      text: 'Units'
    }
  },
  tooltip: {
    formatter: function () {
      return '<b>' + this.series.name + '</b><br/>' +
        this.point.y + ' ' + this.point.name.toLowerCase();
    }
  }
});




  Highcharts.chart('cc', {
  data: {
    table: 'datatable3',
  },
  chart: {
    type: 'pie'
  },
  title: {
    text: 'Nombre voyage_organise/voyage'
  },
  yAxis: {
    allowDecimals: false,
    title: {
      text: 'Units'
    }
  },
  tooltip: {
    formatter: function () {
      return '<b>' + this.series.name + '</b><br/>' +
        this.point.y + ' ' + this.point.name.toLowerCase();
    }
  }

}

);












  </script>
   {% endblock %}

   {% endblock %}