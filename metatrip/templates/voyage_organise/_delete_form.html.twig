<form method="post" action="{{ path('app_voyage_organise_delete', {'idvo': voyage_organise.idvo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage_organise.idvo) }}">
    <button class="btn">Delete</button>
</form>
